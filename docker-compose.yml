version: "3.9"

services:
  nemesisnet-dev:
    image: nemesisguy/nemesisnet:dev
    container_name: nemesisnet-dev
    restart: unless-stopped
    ports:
      - "9114:3000"
    env_file:
      - .env